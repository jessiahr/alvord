defmodule Alvord.RepoTest do
  use ExUnit.Case
  alias Alvord.Repo
  alias Alvord.Store
  doctest Repo

  # test "it gets a package from remote" do
  #   assert Repo.get_file_from_repo("jessiahr/alvord") == 1
  # end
end
